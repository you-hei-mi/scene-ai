# 26.1.1 - Vue3 + Nuxt4 迁移后清理开发计划

## 项目现状概览

- 技术栈迁移完成度：**90%**
  - Vue3 + Nuxt4 前端迁移基本完成，所有页面组件已迁移
  - Tauri 桌面应用配置完成度 **85%**，基本配置就绪
  - Langflow 集成：**0%**，尚未实现
- UI 重构：玻璃态设计系统已应用到所有页面，设计风格统一
- 代码结构：monorepo 结构清晰，但存在残留问题需要清理

## 问题清单（已识别）

### P0 - 高优先级（必须完成）

| ID | 问题 | 位置 | 预估工作量 | 影响 |
|---|---|---|---|---|
| P0-001 | 移除 pnpm-workspace.yaml 中残留的 React 依赖 | `pnpm-workspace.yaml:68-96` `catalog:web` 中 | 10 分钟 | 避免混淆依赖，减小锁文件体积 |
| P0-002 | 删除根 package.json 中残留的 React 依赖 | `package.json:96-113` | 10 分钟 | 清理依赖，避免安装不必要包 |
| P0-003 | 移除模板中残留的 React 模板 | `templates/extension-starter/package.json` | 20 分钟 | 确保新扩展模板使用正确的 Vue 依赖 |
| P0-004 | 更新 docker-compose.yml 中已删除的 volumes 引用 | `docker-compose.yml:73` | 10 分钟 | 修复 volume 挂载指向不存在目录 |
| P0-005 | 更新 .prettierrc.cjs 中已删除的路径 | `.prettierrc.cjs:15` | 5 分钟 | 修复格式化配置 |
| P0-006 | 更新 scripts/release.mjs 路径判断 | `scripts/release.mjs:18-19,56-61` | 20 分钟 | 修复发布脚本指向正确目录 |
| P0-006 | 更新根 package.json 中脚本 | `package.json:68-72` | 10 分钟 | 删除指向 packages/client 的脚本 |
| P0-007 | 将 `@buildingai/web-vue/services` 中的 mock 服务对接真实后端 API | `packages/@buildingai/web-vue/services/*.ts` 所有文件 | 2 小时 | 所有前端页面当前使用 mock，需要对接真实后端 |
| P0-008 | 更新 26.1.1-开发计划.md 进度 | `docs/todo/26.1.1-开发计划.md` | 10 分钟 | 文档同步 |

### P1 - 中优先级（建议完成）

| ID | 问题 | 位置 | 预估工作量 | 影响 |
|---|---|---|---|---|
| P1-001 | 统一工具函数到 `@buildingai/utils` | 检查各处重复的工具函数 | 1 小时 | 减少重复代码，便于维护 |
| P1-002 | 统一 UI 组件到 `@buildingai/web-vue/ui` | `apps/web-nuxt/app/components` 已有组件抽取 | 2 小时 | 让扩展也能复用前端组件 |
| P1-003 | 检查并清理 ES 链接配置 | `eslint.config.mjs` | 20 分钟 | 移除对已删除包的引用 |
| P1-004 | 验证 tsconfig 路径配置正确 | `tsconfig.json` 各层 | 30 分钟 | 确保路径别名正确 |
| P1-005 | 运行 pnpm dedupe 清理重复依赖 | 整体工作区 | 10 分钟 | 减小 node_modules 体积 |

### P2 - 低优先级（后续迭代）

| ID | 问题 | 位置 | 预估工作量 | 影响 |
|---|---|---|---|---|
| P2-001 | Langflow 集成准备 | 新建包结构 | 1-2 天 | 后续功能迭代 |
| P2-002 | Tauri 桌面端功能测试 | 验证端到端 | 4 小时 | 确保桌面应用可用 |
| P2-003 | 性能优化 - 路由懒加载 | Nuxt 已自动处理，验证 | 30 分钟 | 提升首屏加载速度 |
| P2-004 | 性能优化 - 图片懒加载 | 检查所有图片 | 30 分钟 | 减少不必要加载 |

## 开发顺序建议

**阶段一：依赖清理（约 1 小时）**

1. P0-001 → P0-002 → P0-003 → P0-004 → P0-005 → P0-006
2. 运行 `pnpm install` 更新锁文件
3. 验证安装成功，无错误

**阶段二：API 对接（约 2 小时）**

1. P0-007 逐个服务对接：
   - `user.ts` → 登录/注册/获取用户信息
   - `agent.ts` → 智能体列表/创建/更新/删除
   - `chat.ts` → 聊天消息/对话历史
   - `dataset.ts` → 知识库列表/创建/删除
2. 检查前端 `useApiFetch` 已正确配置 baseURL
3. 测试各页面功能正常

**阶段三：代码整理（约 3 小时）**

1. P1-001 抽取重复工具
2. P1-002 抽取共用 UI 组件
3. P1-003 → P1-004 配置清理
4. P1-005 去重

**阶段四：文档更新（10 分钟）**

1. P0-008 更新开发计划进度
2. 验证 git status 确保只修改预期文件

## 验证标准

完成所有任务后，应通过以下验证：

- [ ] `pnpm install` 正常完成，无警告错误
- [ ] `pnpm dev` 能正常启动 web-nuxt
- [ ] 所有页面能正常加载，无 404
- [ ] 登录流程正常工作
- [ ] 智能体列表能正确从后端获取
- [ ] 知识库列表能正确从后端获取
- [ ] ESLint 检查通过：`pnpm lint`
- [ ] 类型检查通过：`pnpm check-types`

## 风险提示

1. **API 对接需要后端服务运行**，请确保后端 API 已启动且可访问
2. **依赖清理后** 如果出现找不到模块错误，检查 pnpm-lock.yaml 重新生成
3. **脚本文件引用** 仅需更新路径，不需要改变原有逻辑

## 预计总工时

- P0 高优先级：**~3 小时**
- P1 中优先级：**~3.5 小时**
- P2 低优先级：**~1-2 天**

总计：约 6.5 小时（不含 P2）
